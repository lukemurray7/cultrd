# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your-supabase-project-url
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key

# OAuth Redirect URL (for password reset and OAuth callbacks)
# Development with Expo Go (same machine): exp://localhost:8081
# Development with Expo Go (physical device): exp://YOUR_LOCAL_IP:8081
# Production/Custom build: cltr://
EXPO_PUBLIC_APP_URL=exp://localhost:8081

# Apple Sign-In Configuration (optional, only needed if using Apple Sign-In)
EXPO_PUBLIC_APPLE_AUTH_SERVICE_ID=your-apple-service-id
EXPO_PUBLIC_APPLE_AUTH_REDIRECT_URI=cltr://auth/callback
